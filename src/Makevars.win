## Use OpenMP (for RcppParallel)
PKG_CPPFLAGS += $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS += $(SHLIB_OPENMP_CXXFLAGS)

## Link LAPACK and BLAS on Windows
PKG_LIBS += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)